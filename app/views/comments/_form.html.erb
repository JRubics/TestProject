<h3>Comments (<%= @post.comments.size %>)</h3>

<%= form_for [ @post, @comment ] do |f| %>
  <p>
    <%= f.label :text, "Your comment" %><br/>
    <%= f.text_area :text %>
  </p>
  <p>
    <%= f.submit 'Submit' %>
  </p>
<% end %>